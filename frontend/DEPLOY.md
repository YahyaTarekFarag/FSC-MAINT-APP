# دليل نشر التطبيق على Vercel 🚀

لضمان عمل التطبيق بشكل صحيح عند النشر (Production)، يرجى اتباع الخطوات التالية في لوحة تحكم Vercel:

## 1. إعداد متغيرات البيئة (Environment Variables)
يجب إضافة المتغيرات التالية في إعدادات المشروع (Settings > Environment Variables):

| اسم المتغير | الوصف |
| :--- | :--- |
| `VITE_SUPABASE_URL` | رابط مشروع Supabase الخاص بك |
| `VITE_SUPABASE_ANON_KEY` | مفتاح API العام (Anon Key) |

## 2. إعدادات البناء (Build Settings)
بشكل افتراضي، سيتعرف Vercel على مشروع Vite تلقائياً، ولكن تأكد من الإعدادات التالية:
- **Build Command:** `npm run build`
- **Output Directory:** `dist`

## 3. ملاحظات هامة
- تم إضافة ملف `vercel.json` للتعامل مع مسارات التطبيق (SPA Routing) لضمان عدم ظهور خطأ 404 عند تحديث الصفحة.
- التطبيق يدعم PWA، لذا سيظهر خيار التثبيت للمستخدمين فور النشر بنجاح.
